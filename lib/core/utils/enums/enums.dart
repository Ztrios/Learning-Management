enum InternetConnection {connected, disconnected}
enum Status { initial, loading, success, failure, error }
enum RecipeType { all, instants, schedule }
enum ParentCategory { instants, schedule }
enum OrderType { regular,campaign }
enum CampaignType { discount,cashback,undefined }