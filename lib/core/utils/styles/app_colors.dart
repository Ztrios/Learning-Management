import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Prevent instantiation

  // Brand Colors
  static const Color deepBlue = Color(0xFF0E64D2);
  static const Color blueLight = Color(0xFF008CFF); // Alias for deepBlue
  static const Color deepOrange = Color(0xFFFF7648);
  static const Color deepPurpleAccent = Color(0xFF7364E6);
  static const Color deepGreen = Color(0xFF0D6E58);

  // UI Support Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF212525);
  static const Color textGrey = Color(0xFF636363);
  static const Color borderColor = Color(0xFF9B9B9B);

  // Status Colors
  static const Color errorRed = Color(0xFFB00020);

  // Background
  static const Color background = Color(0xFFFFFFFF);

}
